# Test Environment Configuration for LLM Integration Tests
# Copy this file to .env.test and fill in your API keys

# IMPORTANT: Never commit .env.test with real API keys to version control!
# The .gitignore file should include .env.test

# ============================================================================
# LLM Provider API Keys (Required for Integration Tests)
# ============================================================================

# OpenAI API Key
# Get from: https://platform.openai.com/api-keys
# Required for: OpenAI provider tests (gpt-4.1-mini, gpt-5, etc.)
OPENAI_API_KEY=sk-...

# Anthropic API Key
# Get from: https://console.anthropic.com/settings/keys
# Required for: Anthropic provider tests (claude-3-5-haiku-20241022, etc.)
ANTHROPIC_API_KEY=sk-ant-...

# Google API Key (for Gemini)
# Get from: https://makersuite.google.com/app/apikey
# Required for: Gemini provider tests (gemini-2.0-flash-exp, gemini-1.5-pro, etc.)
GOOGLE_API_KEY=...

# ============================================================================
# LLM Configuration (Optional - Defaults Provided)
# ============================================================================

# Allowed models for testing (comma-separated)
# Default: All models from MODEL_PROVIDER_MAP
ALLOWED_MODELS=gpt-4.1-mini,gpt-4.1,gpt-5,gpt-5-mini,claude-3-5-haiku-20241022,claude-3-5-sonnet,claude-3-opus,gemini-2.0-flash-exp,gemini-1.5-pro,gemini-2.0-flash-exp

# Request timeout in seconds (default: 60)
LLM_REQUEST_TIMEOUT=60

# Maximum retry attempts (default: 3)
LLM_MAX_RETRIES=3

# ============================================================================
# Test Environment Settings
# ============================================================================

# Enable debug logging for tests
DEBUG=false

# Log level for tests (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# ============================================================================
# Rate Limiting (Optional)
# ============================================================================
# Note: Integration tests may hit rate limits. Consider these settings:

# For OpenAI:
# - Tier 1: 500 RPM, 30,000 TPM
# - Tier 2: 5,000 RPM, 450,000 TPM
# - See: https://platform.openai.com/account/rate-limits

# For Anthropic:
# - Tier 1: 50 RPM, 40,000 TPM
# - Tier 2: 1,000 RPM, 80,000 TPM
# - See: https://console.anthropic.com/settings/limits

# For Google Gemini:
# - Free tier: 15 RPM, 1M TPM
# - Paid tier: 1,000 RPM, 4M TPM
# - See: https://ai.google.dev/pricing

# ============================================================================
# CI/CD Configuration
# ============================================================================
# For GitHub Actions or other CI systems:
# - Set these as repository secrets
# - Tests will be skipped if keys are not available
# - Use separate test accounts with usage limits

# ============================================================================
# Usage Instructions
# ============================================================================
# 1. Copy this file to .env.test:
#    cp .env.test.template .env.test
#
# 2. Fill in your API keys in .env.test
#
# 3. Run integration tests:
#    uv run pytest tests/integration/test_llm_integration.py -v -m integration
#
# 4. Run specific provider tests:
#    uv run pytest tests/integration/test_llm_integration.py::TestProviderIntegrationOpenAI -v
#
# 5. Skip tests without API keys:
#    Tests automatically skip if API keys are not set
#
# 6. Monitor usage:
#    - Check provider dashboards for usage/costs
#    - Set up billing alerts
#    - Use test accounts with rate limits
