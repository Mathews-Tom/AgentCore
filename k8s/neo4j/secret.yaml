apiVersion: v1
kind: Secret
metadata:
  name: neo4j-secret
  namespace: agentcore
  labels:
    app: neo4j
    component: memory-system
type: Opaque
stringData:
  # Format: username/password
  # Default dev credentials (CHANGE IN PRODUCTION)
  auth: "neo4j/password"
  password: "password"
