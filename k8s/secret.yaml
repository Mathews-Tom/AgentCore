apiVersion: v1
kind: Secret
metadata:
  name: a2a-protocol-secrets
  namespace: agentcore
type: Opaque
stringData:
  # Generate these with: kubectl create secret generic a2a-protocol-secrets --from-literal=...
  # Or use external secrets management (Vault, AWS Secrets Manager, etc.)
  JWT_SECRET_KEY: "CHANGE_ME_IN_PRODUCTION"
  POSTGRES_USER: "agentcore"
  POSTGRES_PASSWORD: "CHANGE_ME_IN_PRODUCTION"